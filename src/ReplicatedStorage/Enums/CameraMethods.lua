--Need to ensure that the order and spelling matches the CameraController
local CameraActions = {
	setTarget = "setTarget",
	setDistance = "setDistance",
	transitionTo = "transitionTo",
	initializeCameraDrag = "initializeCameraDrag",
	dragCamera = "dragCamera",
	endDrag = "endDrag"
}

return CameraActions
