--for instance, if targetchoice = Enemy, and targetType = First then it'll look to first enemy, and if effectchoice = enemy then it'll do damage around that

local CardTypes = {
	ENEMY = "Enemy",
	ALLY = "Ally",
	ANY = "Any",
	NONE = "None"
}

return CardTypes
