--these actions get requested from the client to server that the player wants to do

local Actions = {
	REQUEST_EVENT_OPTION = "Request Event Option",
	REQUEST_END_GAME = "Request End Game"
}

return Actions
